// Code your solutions in this file
const workers = ['Lisa', 'Kaitlin', 'Jan']

function printBadges(worker){
  let workers = worker
  for (let i = 0; i < workers.length; i++) {
    console.log(`Welcome ${workers}! You are employee #1.`)
  }
  return worker
}
